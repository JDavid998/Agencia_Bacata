<nav class="floating-nav z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6">
    <div class="flex justify-between items-center h-16">
      <div class="flex items-center space-x-4">
        <a href="<%= root_path %>" class="text-indigo-700 font-bold text-2xl tracking-tight">Agencia Bacata</a>
        <div class="hidden md:flex items-center space-x-6 ml-8">
          <a href="<%= root_path %>" class="text-sm text-gray-700 hover:text-indigo-600 transition">Inicio</a>
          <a href="<%= about_path %>" class="text-sm text-gray-700 hover:text-indigo-600 transition">Quiénes somos</a>
          <a href="<%= services_path %>" class="text-sm text-gray-700 hover:text-indigo-600 transition">Servicios</a>
          <a href="<%= planes_path %>" class="text-sm text-gray-700 hover:text-indigo-600 transition">Planes</a>
          <a href="<%= clientes_path %>" class="text-sm text-gray-700 hover:text-indigo-600 transition">Nuestros clientes</a>
          <a href="<%= contacto_path %>" class="text-sm text-gray-700 hover:text-indigo-600 transition">Contacto</a> 
        </div>
      </div>  
      <div class="flex items-center space-x-4">
        <% if user_signed_in? %>
          <a href="<%= dashboard_path %>" class="px-4 py-2 rounded-md border border-indigo-600 text-indigo-700 text-sm hover:bg-indigo-50 transition">Dashboard</a>
          <%= button_to 'Salir', destroy_user_session_path, method: :delete, class: "px-4 py-2 rounded-md bg-red-500 text-white text-sm hover:bg-red-600 transition" %>
        <% else %>
          <a href="<%= new_user_session_path %>" class="px-4 py-2 rounded-md bg-indigo-600 text-white text-sm hover:bg-indigo-700 transition">Iniciar sesión</a>
        <% end %>
      </div>
    </div>
  </div>
</nav>
